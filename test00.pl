#!/usr/bin/perl -w

foreach $thing (@list) {
	$string .= $separator . $thing;
}
return $string;
$course =~ s/\|.*//;
$count{$course} = $1;
$count{$course} = $1;
$count{$course} = $1;
$ffff{} = dlkskd;